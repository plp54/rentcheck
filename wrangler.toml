name = "rentcheck"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

[env.production]
vars = { NEXT_PUBLIC_URL = "https://rentcheck.pages.dev" }

# Secrets à configurer via l'interface Cloudflare ou wrangler secret put
# STRIPE_SECRET_KEY
