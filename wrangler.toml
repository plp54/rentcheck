name = "rentcheck"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

[env.production]
vars = { NEXT_PUBLIC_URL = "https://loyerlegal.be" }

# Custom domain: loyerlegal.be
# Configure in Cloudflare Dashboard > Pages > rentcheck > Custom domains
